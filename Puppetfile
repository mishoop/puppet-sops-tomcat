# Puppetfile for r10k module management
# Run: r10k puppetfile install

forge 'https://forgeapi.puppet.com'

# Required modules from Puppet Forge
mod 'puppet-archive', '7.1.0'
mod 'puppetlabs-stdlib', '9.4.1'
mod 'puppetlabs-concat', '9.0.2'

# Local modules (in modules/ directory)
# These are managed in this repo, not pulled by r10k
